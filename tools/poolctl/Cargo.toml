[package]
name = "poolctl"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
image.workspace = true
sha2.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
clap.workspace = true
# The manifest type, its hash rule, the derivation's requirements and the project's one timestamp
# format live in the server crate. A second copy here would be a second thing to keep in step with
# `shared/vectors`, and on the day the two disagreed the tool would publish manifests the server
# draws against differently.
server = { path = "../../server" }
